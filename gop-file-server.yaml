apiVersion: v1
kind: Pod
metadata:
  name: gop-file-server
spec:
  volumes:
    - name: data-store
      persistentVolumeClaim:
        claimName: data-pvc
  containers:
  - name: fileserver
    image: kodekloud/fileserver
    volumeMounts:
    - mountPath: /web
      name: data-store